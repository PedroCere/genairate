server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/genairate-config # o un repo Git remoto
          default-label: main
  application:
    name: config-server

