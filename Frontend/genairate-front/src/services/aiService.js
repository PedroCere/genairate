export const aiService = {
  generateImage: async (prompt) => {
    console.warn('aiService.generateImage called with prompt:', prompt);
    // Return a placeholder image URL
    return 'https://via.placeholder.com/150';
  }
};
